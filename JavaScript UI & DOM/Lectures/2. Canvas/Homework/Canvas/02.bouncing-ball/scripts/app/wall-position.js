define(function () {
    'use strict';

    var WallPosition = {
        'Bottom': 0,
        'Top': 1,
        'Left': 2,
        'Right': 3
    };
    Object.freeze(WallPosition);

    return WallPosition;
});